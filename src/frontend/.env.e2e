# Browser smoke tests intentionally use the deterministic in-memory fixture.
# Email auth stays ON so E2E can exercise the captcha register flow with a
# mocked /user/captcha endpoint (capability-off path is covered by unit tests).
VITE_ENABLE_PROMPT_API=false
VITE_EMAIL_AUTH_ENABLED=true
VITE_GITHUB_OAUTH_ENABLED=false
