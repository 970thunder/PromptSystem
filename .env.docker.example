# Copy to .env for local Docker Compose (never commit .env)
PROMPTOS_FRONTEND_PORT=3000
PROMPTOS_BACKEND_PORT=8080
PROMPTOS_MYSQL_PORT=3307
PROMPTOS_REDIS_PORT=6380
PROMPTOS_REDIS_PASSWORD=
PROMPTOS_MYSQL_ROOT_PASSWORD=change-me-use-a-long-random-password
PROMPTOS_MYSQL_USER=promptos_app
PROMPTOS_MYSQL_PASSWORD=change-me-use-a-long-random-password
PROMPTOS_JWT_SECRET=change-me-use-a-long-random-string
PROMPTOS_JWT_EXPIRE_HOURS=72
MYSQL_MIGRATION_USER=promptos_migrator
MYSQL_MIGRATION_PASSWORD=change-me-use-a-long-random-password
PROMPTOS_DB_MAX_OPEN_CONNS=10
PROMPTOS_DB_MAX_IDLE_CONNS=5
PROMPTOS_DB_CONN_MAX_LIFETIME_MINUTES=30

# Production browser sessions use an HttpOnly cookie; keep enabled.
AUTH_COOKIE_ENABLED=true

PROMPTOS_UPLOAD_PROVIDER=local
PROMPTOS_UPLOAD_BASE_URL=http://localhost:8080
PROMPTOS_UPLOAD_MAX_MB=10
PROMPTOS_UPLOAD_MAX_CONCURRENT=4
PROMPTOS_UPLOAD_DAILY_QUOTA_MB=100
PROMPTOS_UPLOAD_TOTAL_QUOTA_MB=2048
# GIF is disabled by default (animated GIFs are hard to validate safely); opt in only if needed
PROMPTOS_UPLOAD_ALLOW_GIF=false
# Frontend capability switches are consumed at image build time.
VITE_GITHUB_OAUTH_ENABLED=false
VITE_SKILL_ENABLED=false
VITE_PLAYGROUND_ENABLED=false
VITE_CREATOR_ACADEMY_ENABLED=false
VITE_MARKETPLACE_ENABLED=false
# Comma-separated HTTPS domains allowed for remote prompt cover/images (default: only local /uploads/)
PROMPTOS_ALLOWED_IMAGE_DOMAINS=

# GitHub OAuth — use real values only in .env (not here)
GITHUB_CLIENT_ID=your-github-oauth-app-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-app-client-secret
GITHUB_OAUTH_ENABLED=false
GITHUB_REDIRECT_URI=http://localhost:8080/api/v1/auth/github/callback
FRONTEND_URL=http://localhost:3000

# Unified account (Keycloak OIDC). Use a separate confidential client per site.
OIDC_ENABLED=false
OIDC_ISSUER=https://id.example.com/realms/example
OIDC_CLIENT_ID=promptsystem-web
OIDC_CLIENT_SECRET=replace-with-secret
OIDC_REDIRECT_URI=http://localhost:8080/api/v1/auth/oidc/callback

PROMPTOS_R2_ACCOUNT_ID=
# RustFS/S3-compatible endpoint, e.g. http://rustfs:9000. Leave empty for Cloudflare R2.
PROMPTOS_R2_ENDPOINT=
PROMPTOS_R2_ACCESS_KEY_ID=
PROMPTOS_R2_SECRET_ACCESS_KEY=
PROMPTOS_R2_BUCKET=
PROMPTOS_R2_PUBLIC_URL=
